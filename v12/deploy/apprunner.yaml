version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - echo "Static dashboard - no build required"
run:
  command: npm start
  env:
    - name: NODE_ENV
      value: production
    - name: PORT
      value: "3000"
